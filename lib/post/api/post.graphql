query Post($groupId: String!, $postId: String!) {
    postsFeedItem(groupId: $groupId, postId: $postId) {
        id
        url
        group {
          id
          name
          groupPhotoUrl
        }
        creationDate
        description
        photoAttachmentUrls
    }
}