const mockLocationLabel = 'City of the user';
const mockLocationKey = 'userSearchLocationKey';

const locationsSearchResultWithTargetOption = <String, dynamic>{
  'locations': [
    {
      'label': mockLocationLabel,
      'key': mockLocationKey,
      'keyType': 'MOCK_KEY_TYPE',
    },
  ],
};
